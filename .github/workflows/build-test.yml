name : My First Workflow

on:
  - push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: echo HELLO
        name: Say Hello
